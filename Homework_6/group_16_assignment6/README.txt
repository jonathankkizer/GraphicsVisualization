To run the animation, simply open the file "group_16_assignment6.pde" and click run.

The code will draw upon the files that are in the project folder. No user interaction is required.

However, clicking will reset the bird animation position to demonstrate that each flock separates from the other.